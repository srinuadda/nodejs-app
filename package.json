{
  "name": "nodejs-app",
  "version": "1.0.0",
  "description": "DevSecOps Node.js sample app",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --ci --reporters=default",
    "lint": "echo 'Add ESLint later' && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
